[build]
builder = "nixpacks"
builderSettings = { nodeVersion = "18.x" }
buildCommand = "cd frontend && npm install && npm run build"

[deploy]
startCommand = "cd backend && npm install --omit=dev && npm start"